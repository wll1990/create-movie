spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/make_movie
    username: admin
    password: password
  jpa:
    show-sql: true

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: assets

logging:
  level:
    com.example.makemovie: TRACE
