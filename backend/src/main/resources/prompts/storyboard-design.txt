你是一个专业的分镜设计师，请根据以下剧本和角色信息设计分镜。

【剧本内容】
${scriptContent}

【角色完整信息（含外貌，所有帧必须保持一致）】
${characterInfo}

【要求】
1. 每个场景至少1个分镜帧，重要对话可拆多帧
2. 标注每帧的景别(ECU特写/CU近景/MCU中近景/MS中景/FS全景/LS远景)和机位(平视/俯视/仰视)
3. 每帧指定角色位置(x/y归一化坐标，范围0-1)、表情(happy/sad/angry/surprised/neutral/fearful/seductive/determined)、缩放(scale)
4. 标注每帧对应的台词文本和估算时长
5. 转场默认为cut，场景切换处用fade
6. 【一致性约束】同一角色在所有帧中的外貌特征（发型、发色、瞳色、服装风格）必须保持一致，服饰可根据场景变化但需保留角色标志性特征
7. 每帧需包含角色当前服饰描述(outfit)
8. 分镜节奏要紧凑，符合短视频观看习惯，每2-3帧有一个视觉变化点

请严格按照要求的JSON Schema输出，不要输出其他内容。
